namespace StripeBridge
{
    namespace Enums
    {
        /** Diffrent operation modes for the stripe. */
        enum StripeModes
        {
            /** Turns the whole stripe off. */
            Off,
            /** Static (freezed) effects. */
            Static,
            /** Moving effects. */
            Effect,
        };
    } // namespace Enums
} // namespace StripeBridge